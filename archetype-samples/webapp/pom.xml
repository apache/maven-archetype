<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.apache.maven.archetypes</groupId>
        <artifactId>archetype-samples</artifactId>
        <version>2.0-alpha-5-SNAPSHOT</version>
    </parent>

    <artifactId>maven-webapp</artifactId>
    <version>2.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <name>Maven Webapp Sample</name>

    <profiles>
        <!--
          `mvn -Prun` builds the war and serves it on http://localhost:15000/.

          Cargo is container-neutral: swapping the containerId below runs the same
          war on Jetty, WildFly, Payara and so on without touching the rest of the
          pom. Tomcat 9 is the default here because this sample's web.xml declares
          servlet 2.5 in the javax.servlet namespace; Tomcat 10+ and Jetty 11+ moved
          to jakarta.servlet and would not deploy it unchanged.
        -->
        <profile>
            <id>run</id>
            <build>
                <defaultGoal>cargo:run</defaultGoal>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.cargo</groupId>
                        <artifactId>cargo-maven3-plugin</artifactId>
                        <version>1.10.28</version>
                        <configuration>
                            <container>
                                <containerId>tomcat9x</containerId>
                                <type>embedded</type>
                            </container>
                            <configuration>
                                <properties>
                                    <cargo.servlet.port>15000</cargo.servlet.port>
                                </properties>
                            </configuration>
                            <deployables>
                                <deployable>
                                    <properties>
                                        <context>/</context>
                                    </properties>
                                </deployable>
                            </deployables>
                        </configuration>
                        <dependencies>
                            <dependency>
                                <groupId>org.apache.tomcat.embed</groupId>
                                <artifactId>tomcat-embed-core</artifactId>
                                <version>9.0.121</version>
                            </dependency>
                            <dependency>
                                <groupId>org.apache.tomcat.embed</groupId>
                                <artifactId>tomcat-embed-jasper</artifactId>
                                <version>9.0.121</version>
                            </dependency>
                            <dependency>
                                <groupId>org.apache.tomcat</groupId>
                                <artifactId>tomcat-util-scan</artifactId>
                                <version>9.0.121</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
